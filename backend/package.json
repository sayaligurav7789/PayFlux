{
  "name": "payment-orchestration-backend",
  "version": "1.0.0",
  "description": "Idempotent payment transaction orchestration API",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/config/runMigrations.js",
    "test": "jest --runInBand",
    "test:watch": "jest --watch --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "ioredis": "^5.4.1",
    "dotenv": "^16.4.5",
    "express-rate-limit": "^7.2.0",
    "rate-limit-redis": "^4.2.0",
    "uuid": "^9.0.1",
    "pino": "^9.0.0",
    "pino-http": "^10.0.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 15000
  }
}
